{
  "name": "soop-integrated-server",
  "version": "1.0.0",
  "description": "SOOP live status + notice crawler server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "express": "^4.19.2",
    "puppeteer": "^22.15.0"
  }
}